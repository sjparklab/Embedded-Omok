```markdown
특징:
- 보드 크기: 19×19 (기본값)
- 금수 규칙(Renju) 미적용: 단순 5목 이상이면 승리 처리
- AI가 선(흑)으로 시작할 경우에도 보드가 비어있으면 첫 수를 중앙(9,9)으로 둡니다.
- 성능 향상 기법:
  - 루트에서 Dirichlet 노이즈 적용(탐색 다양성 향상)
  - bounding-box 기반 후보 절단으로 MCTS 시뮬레이션 집중
  - self-play 데이터 증강(회전+반전)으로 일반화 향상
```
